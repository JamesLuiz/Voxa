{
  "version": 2,
  "buildCommand": "npm --prefix Voxa/backend ci && npm --prefix Voxa/backend run build",
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node@2.9.1" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" }
  ]
}